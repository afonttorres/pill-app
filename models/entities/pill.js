export class Pill {
    shape;
    color;
    name;
    when;

    constructor(shape, color, name, when) {
        this.shape = shape;
        this.color = color;
        this.name = name;
        this.when = when;
    }
}